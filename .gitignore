/vendor
/.idea
/.vscode
storage/installed
# git has no inline comments -- the trailing text was part of the pattern,
# so this rule matched nothing at all. Kept as an ignore because uploads
# do not belong in the repo; hero-login.jpg stays tracked because it
# already is. Seeder fixtures live in database/seeders/fixtures/ instead,
# outside the docroot.
public/images/
*.log           # Example for log files
.env
# any env variant (backups, per-client) — never commit; keep only *.example
.env.*
!.env.example
!.env.*.example
node_modules
/storage/framework
/storage
/storage_old
run-tickets.ps1
/public/build
/public/hot
/public/storage
.phpunit.result.cache
/tmp_itc.php
/_review_branches.txt
/_review_done.txt
/_review_fetch.txt
/.claude/worktrees/
/design_handoff_booking_flow
# Local database dumps (may contain production / customer data — never commit)
*.sql
/bangspbp_car_directonderweg.sql
